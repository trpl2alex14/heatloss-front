export type Request = {
	id?: number,
	lead?: number,
	deal?: number,
	name?: string,
	phone?: string,
	email?: string,
	comment?: string
};

export type Attach = {
	label: string,
	url: string
}
